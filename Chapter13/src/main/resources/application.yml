server:
  port: 9898

logging:
  path: logs
  file: userManager.log

spring:
  application:
    name: userManager
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/course?serverTimezone=GMT
    username: root
    password: wjs123456

mybatis:
  type-aliases-package: com.course.model
  mapper-locations:
    - mapper/*

